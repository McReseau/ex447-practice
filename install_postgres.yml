---
- hosts: databases
  become: true
  tasks:
  - yum:
      name: 
        - postgres
        - postgres-contrib
        - postgres-server
      state: latest
  - shell: cmd="postgresql-setup --initdb"
  - systemd:
      name: postgresql
      state: started
      enabled: true
