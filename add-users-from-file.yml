---
- hosts: all
  become: true
  vars:
    f: "{{ q('file','userslist.yml')}}"
  tasks:
    - user:
        name: "{{ item.name }}"
        uid: "{{ item.uid | int }}"
        password: "{{ item.pwd | password_hash('sha512') }}"  
      loop: "{{ f }}"
