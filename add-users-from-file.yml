---
- hosts: all
  become: true
  tasks:
    - user:
        name: "{{ item.users.name }}"
        uid: "{{ item.users.uid | int }}"
        password: "{{ item.users.pwd | password_hash('sha512') }}"  
      with_file:
        - userslist.yml
