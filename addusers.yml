---
- hosts: all
  become: true
  vars:
    userlist:
      - name: bob
        pwd: bob-pw
      - name: jim
        pwd: jim-ww
  tasks:
    - user:
        name: "{{ item.name }}"
        password: "{{ item.pwd | password_hash('sha512') }}"  
      loop: "{{ userlist }}"
